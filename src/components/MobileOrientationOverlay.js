import React from 'react';
import './MobileOrientationOverlay.css';

// Completely disabled component - will never show the overlay
const MobileOrientationOverlay = () => {
  // Always return null to disable the component entirely
  return null;
};

export default MobileOrientationOverlay;